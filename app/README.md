# Application code

The application stack and final source layout have not been selected yet.

When the Technical Specification chooses them, place the application code here or update the root routing files to point at the chosen location.
